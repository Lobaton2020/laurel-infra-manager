apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Ancla todos los recursos al namespace `prod` (ya existente) sin duplicarlo.
namespace: prod

resources:
  - jenkins-deployment.yml
  - jenkins-service.yml
  - jenkins-ingress.yml