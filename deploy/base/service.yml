apiVersion: v1
kind: Service
metadata:
  name: laurel-infra-manager
spec:
  selector:
    app: laurel-infra-manager
  ports:
    - port: 3006
      targetPort: 5002
      protocol: TCP
      name: http
  type: LoadBalancer